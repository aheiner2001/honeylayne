/// Flip this to `true` after you have:
///   1. Created a Firebase project,
///   2. Run `flutterfire configure` (generates lib/firebase_options.dart),
///   3. Enabled Firestore + Storage in the Firebase console.
///
/// While false, the app stores everything locally in the browser so it
/// runs with zero setup. See README.md for full steps.
const bool kUseFirebase = true;

rules_version = '2';

// Honey Layne: anyone can read the catalog + site settings; only a signed-in
// manager (Firebase Auth) can write.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}

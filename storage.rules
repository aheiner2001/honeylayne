rules_version = '2';

// Honey Layne product/site photos: public read, writes only by a signed-in
// manager (Firebase Auth).
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
